i#include "monty.h"

/**
 * nop -  does not do anything
 * @stack: the stack given by the main (which won't be used)
 * @line_cnt : similar to the previous argument
 *
 * Return: nothing
 */

void nop(stack_t **stack, unsigned int line_cnt)
{
	(void) stack;
	(void) line_cnt;
}
